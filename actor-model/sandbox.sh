#!/usr/bin/env bash

# TODO setup multiple concurrent processes that communicate via named pipes
# TODO setup main loop process to step through functions, create multiple functions with support for a "step" argument and a message argument, messages should be written to queue files (read from beginning, write to end)
