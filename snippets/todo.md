# TODOs

Create snippet script for markdown syntax

Will probably prototype in bash, but with a go implementation I can play around with disabling the go garbage collector at compile time. The resulting binary doesn't need to worry about being a long running process since it should start up and terminate pretty fast.
